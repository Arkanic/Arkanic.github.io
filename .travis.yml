os: osx
dist: xenial
language: bash
before_install:
  - source envs.sh
script:
  - sudo apt-get install nodejs
  - chmod +x ./sitemap.sh
  - ./sitemap.sh
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $github_token
  keep-history: true
  verbose: true
  on:
    branch: master